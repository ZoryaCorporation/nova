# nova
Nova is a register-based bytecode virtual machine and scripting language written from scratch in C99. Where most scripting languages leave the hard problems to third-party packages — data formats, async I/O, text analysis, build tooling — Nova ships them as first-class capabilities in a single binary.
